Общее:
    Начало работы над данным проектом: 13.11.2023
    Сервис не до конца готов и не протестирован. На данный момент можно оценить только общий дизайн, стиль кода. В ближайшие дни доработаю буду дорабатывать проект.
    Немного изменен формат ответа и путь (можно посмотреть в коде и измененном файле specification).
    Просьба оставить в целом фидбек в случае любого решения по вакансии: что не так, как надо делать. В частности уточнить как здесь можно использовать очередь (RMQ).



Общая идея:
1) приходит запрос на просмотр цены символов.
2) Система смотрит есть ли по указанным символам данные в редис
3) Если все есть - возвращает.
Для недостоющих данных:
4) Система смотрит есть ли открытый вебсокет на каком-либо из провайдеров (потом сделать несколько объектов/сокетов для каждего провайдера)
5) Если нет, то открывается вебсокет, если есть - берется он
6) По вебсокету делается запрос.
7) Ответ записывается в redis и возвращается пользователю.

Текущие проблемы:
1) RuntimeError ('cannot call recv while another coroutine is already waiting for the next message',)
(Сделать asyncio.queue? 
Увеличение количества объектов провайдеров(->сокетов) позволит обрабатывать больше запросов, но все равно проблема не уйдет: если прийдет больше запросов чем есть сокетов)
2) Если сокет открывается (а не уже открыт/еще закрыт) а второй запрос уже на него пришел - как подождать открытия. (найти в библиотеке что-то; на крайней случай - sleep) 
3) Cинхронный редис (но по идее и так быстро, не обязательно?)
4) Логирование
5) Пока не понятно куда тут впихнуть RMQ

